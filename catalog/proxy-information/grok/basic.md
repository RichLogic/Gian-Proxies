# Grok Build：基本信息审阅

基本信息固定展示 Runtime 路径、Runtime 版本和 Proxy 版本。当前发布目标为 Proxy **0.3.6** 与 Grok CLI **1.0.41**；它不表示用户机器已经完成安装或登录。

目标位置：

```text
<dataDir>/runtimes/grok/1.0.41/<artifact-sha256>/bin/grok
```

Agent HOME、账号状态和原生 Session 不属于签名 Catalog，也不能从另一份 `~/.grok` 自动继承。
